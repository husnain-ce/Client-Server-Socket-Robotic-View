
CREATE TABLE USERS (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  email VARCHAR(100) NOT NULL,
  passwd VARCHAR(100) NOT NULL
);

CREATE TABLE MESSAGE (
  id INT AUTO_INCREMENT PRIMARY KEY,
  MESSAGE VARCHAR(200) NOT NULL,
  MSG_ID INT NOT NULL,
  CONSTRAINT `FK_MSG_USERS`
    FOREIGN KEY (MSG_ID) REFERENCES USERS (id)
);


INSERT INTO USERS (name, email, passwd) VALUES ('haroon','tech@gmail.com', 'test');


INSERT INTO MESSAGE (MESSAGE, MSG_ID)
VALUES('OK SO LET ME KNOW',1);

SELECT USERS.id, USERS.name,USERS.email, USERS.passwd, MESSAGE.MESSAGE 
FROM USERS INNER JOIN MESSAGE ON USERS.id=MESSAGE.MSG_ID

CREATE TABLE MESSAGE
(
id INT AUTO_INCREMENT PRIMARY KEY
MESSAGE VARCHAR(200) NOT NULL,
IV VARCHAR NOT NULL,
MSG_ID INT NOT NULL,
CONSTRAINT FK_MSG_USERS FOREIGN KEY (MSG_ID)
        REFERENCES USERS (id)
);

CREATE TABLE Engineer
(
Pk_Engineer_Id INT PRIMARY KEY,
FullName VARCHAR(100),
MobileNo CHAR(11),
Fk_Car_Id INT FOREIGN KEY REFERENCES Car(Pk_Car_Id)
);



CREATE TABLE MESSAGE 
   (
      id INT AUTO_INCREMENT PRIMARY KEY
      , CONSTRAINT FK_TempSales_SalesReason FOREIGN KEY (TempID)
      CONSTRAINT FK_TempSales_SalesReason FOREIGN KEY (TempID)
        REFERENCES Sales.SalesReason (SalesReasonID)
        
   )
;

